<Window x:Class="SampleSpeechLiveAgents.Views.AboutWindow"
        x:Name="window" 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:local="clr-namespace:SampleSpeechLiveAgents.Views"
        xmlns:prop="clr-namespace:SampleSpeechLiveAgents.Properties"
        xmlns:converter="clr-namespace:SampleSpeechLiveAgents.Converters"
        mc:Ignorable="d"
        FontFamily="Meiryo UI"
        Icon="/Images/GAP_ICON2.ico"
        Title="{x:Static prop:Resources.About}"
        Height="370" Width="580"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" WindowStyle="SingleBorderWindow" ResizeMode="NoResize"
        DataContext="{Binding ViewModel, RelativeSource={RelativeSource Mode=Self}}"
        d:DataContext="{d:DesignInstance IsDesignTimeCreatable=False}">
    <Window.Resources>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 製品情報表示 -->
        <Image Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" HorizontalAlignment="Left" Height="52" Margin="18,17,0,10" VerticalAlignment="Top" Width="50" Source="/Images/GAP_ICON2.ico"/>
        <StackPanel Grid.Column="1" Grid.Row="0" Margin="10,17,10,0">
            <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" VerticalAlignment="Top" Text="{Binding AppInfo}"/>
        </StackPanel>
        <TextBlock Grid.Column="1" Grid.Row="1" HorizontalAlignment="Left" Margin="10,5,0,10" TextWrapping="Wrap" VerticalAlignment="Top" Text="{Binding Company}"/>

        <!-- OSS表示 -->
        <TextBlock Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2" Margin="18,0,0,0" Text="{x:Static prop:Resources.OpenSource}" />
        <ScrollViewer Grid.Column="0" Grid.Row="3" Grid.ColumnSpan="2" Margin="18,4,0,10">
            <StackPanel>
                <StackPanel Margin="0,4,0,4">
                    <TextBlock Text="Microsoft Authentication Library (MSAL) for .NET, UWP, NetCore, Xamarin Android and iOS" TextWrapping="Wrap" />
                    <TextBlock Text="COPYRIGHT (C) Microsoft Corporation" />
                    <TextBlock>
                        <Hyperlink NavigateUri="https://github.com/AzureAD/microsoft-authentication-library-for-dotnet/blob/master/LICENSE" RequestNavigate="RequestNavigation">
                            Released under the MIT license
                        </Hyperlink>
                    </TextBlock>
                </StackPanel>
                <StackPanel Margin="0,4,0,4">
                    <TextBlock Text="IdentityModel Extensions for .NET" TextWrapping="Wrap" />
                    <TextBlock Text="COPYRIGHT (C) Microsoft Corporation" />
                    <TextBlock>
                        <Hyperlink NavigateUri="https://github.com/AzureAD/azure-activedirectory-identitymodel-extensions-for-dotnet/blob/dev/LICENSE.txt" RequestNavigate="RequestNavigation">
                            Released under the MIT license
                        </Hyperlink>
                    </TextBlock>
                </StackPanel>
                <StackPanel Margin="0,4,0,4">
                    <TextBlock Text="XamlBehaviors for WPF" TextWrapping="Wrap" />
                    <TextBlock Text="Copyright (c) 2015 Microsoft" />
                    <TextBlock>
                        <Hyperlink NavigateUri="https://github.com/microsoft/XamlBehaviorsWpf/blob/main/LICENSE" RequestNavigate="RequestNavigation">
                            Released under the MIT license
                        </Hyperlink>
                    </TextBlock>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
        <Button Grid.Column="2" Grid.Row="4" Margin="10,0,10,10" Content="{x:Static prop:Resources.OK}" Width="75" Height="27"
                HorizontalAlignment="Right" VerticalAlignment="Top"
                FontWeight="Bold" FontSize="14" Background="White" >
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="Click">
                    <i:CallMethodAction TargetObject="{Binding ElementName=window}" MethodName="Close" />
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </Button>
    </Grid>
</Window>
