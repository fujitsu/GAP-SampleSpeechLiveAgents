<Window x:Class="SampleSpeechLiveAgents.Views.LoginWindow"
        x:Name="window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SampleSpeechLiveAgents.Views"
        xmlns:prop="clr-namespace:SampleSpeechLiveAgents.Properties"
        xmlns:converter="clr-namespace:SampleSpeechLiveAgents.Converters"
        xmlns:app="clr-namespace:SampleSpeechLiveAgents"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        FontFamily="Meiryo UI"
        Title="{x:Static prop:Resources.Login}"
        SizeToContent="Height" Width="550"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" WindowStyle="SingleBorderWindow" ResizeMode="NoResize"
        DataContext="{Binding ViewModel, RelativeSource={RelativeSource Mode=Self}}"
        d:DataContext="{d:DesignInstance IsDesignTimeCreatable=False}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 製品情報表示 -->
        <Image Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" HorizontalAlignment="Left" Height="52" Margin="18,17,0,10" VerticalAlignment="Top" Width="50" Source="/Images/GAP_ICON2.ico"/>
        <StackPanel Grid.Column="1" Grid.Row="0" Margin="10,17,10,0">
            <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" VerticalAlignment="Top" Text="{Binding AppInfo}"/>
        </StackPanel>
        <TextBlock Grid.Column="1" Grid.Row="1" HorizontalAlignment="Left" Margin="10,5,0,10" TextWrapping="Wrap" VerticalAlignment="Top" Text="{Binding Company}"/>

        <TextBlock Grid.Column="1" Grid.Row="2" HorizontalAlignment="Left" Margin="10,5,0,10" TextWrapping="Wrap" VerticalAlignment="Top" 
                   Text="{x:Static prop:Resources.LoginRemarks}"/>

        <StackPanel Grid.Column="1" Grid.Row="3" Margin="4,10,4,4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Margin="0,0,5,0" Content="{x:Static prop:Resources.Login}" Width="88" Height="27"
                    FontWeight="Bold" FontSize="14" Background="White"
                    Command="{Binding LoginCommand}"/>
            <Button Content="{x:Static prop:Resources.Cancel}" Margin="5,0,0,0" Width="88" Height="27"
                    FontWeight="Bold" FontSize="14" Background="White">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <i:CallMethodAction TargetObject="{Binding ElementName=window}" MethodName="Close" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>
        </StackPanel>
    </Grid>
</Window>
